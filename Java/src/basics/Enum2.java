package basics;

// Declaring the enum inside the class
public class Enum2 {

	// Declaring the enum inside the class
	enum Color {
		RED, GREEN, STAR;
	}
	
	public static void main(String[] args) {
		Color c1 = Color.STAR;
		System.out.println(c1);
	}
}
